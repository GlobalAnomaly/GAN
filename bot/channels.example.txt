# Sources for the overnight run. Copy this to channels.txt and edit.
#
#   npx tsx --env-file-if-exists=.env.local bot/run-overnight.ts --sources bot/channels.txt
#
# One per line. A line starting with @ or UC is treated as a YouTube channel and
# walked with playlistItems; anything else is treated as a search.
#
# Prefer channels. A free YouTube key gets 10,000 units a day. Walking a channel
# costs 1 unit per 50 videos; a search costs 100 units per 50. So a channel is
# roughly fifty times cheaper per video, and search is for discovering which
# channels are worth walking rather than for routine fetching.
#
# Lines starting with # are ignored, so a source can be parked without deleting
# the note about why it was here.

@thatUFOpodcast

# Searches cost 100 units each. Uncomment sparingly, and only to find channels.
# rendlesham forest witness interview
# aaro press briefing
